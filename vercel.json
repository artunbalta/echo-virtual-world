{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run build:shared && npm run build -w @echo/web && mkdir -p public && cp -R apps/web/public/. public/",
  "outputDirectory": "apps/web/.next"
}
